#!/usr/bin/env php
<?php

require __DIR__ . '/bootstrap.php';

$client = getShareableClient();

echo 'Registering application with Transunion... ';

$statusCode = $client->register();

echo 'Done!' . PHP_EOL;
